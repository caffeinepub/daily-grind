{
  "frontend/src/App.tsx": "Main TanStack Router app with five routes wrapped in ThemeProvider and AppLayout; the root layout is now a proper named `RootLayout` component (fixing the hooks-rules error) that registers the service worker on mount via useEffect.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — workout schedule CRUD, mark workout complete, mark individual set row complete (new `useMarkSetRowComplete` mutation), random motivational message, user profile get/save, user tier query, and tier evaluation mutation.",
  "frontend/src/components/AppLayout.tsx": "Full app shell with sticky header (logo + nav links including new Settings link + login button), mobile bottom nav bar with Settings icon, main content area, and branded footer.",
  "frontend/src/components/LoginButton.tsx": "Authentication button using useInternetIdentity — shows Login/Logging in.../Logout states, clears React Query cache on logout, handles already-authenticated edge case.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time user profile setup modal with display name input and notifications toggle; now includes the required `currentTier: 0n` and `lastEvaluatedWeek: 0n` fields when saving the profile to satisfy the updated `UserProfile` type.",
  "frontend/src/components/AuthGuard.tsx": "Authentication gate component — shows LoginScreen for unauthenticated users, ProfileSetupModal for users without a profile, loading spinner during initialization.",
  "frontend/src/components/LoginScreen.tsx": "Full-page login landing with app logo, feature highlights list, and LoginButton CTA for unauthenticated users.",
  "frontend/src/pages/TodaysWorkout.tsx": "Today's workout page that renders structured set rows as an interactive checkbox checklist with optimistic UI updates and per-row completion persistence via `useMarkSetRowComplete`; falls back to plain workoutDetails text for legacy entries, and shows a progress counter for set completion.",
  "frontend/src/pages/Schedule.tsx": "Weekly schedule page with per-day edit forms that use a dynamic list of set-row inputs (replacing the plain textarea); each row has a numbered input and remove button, an \"+ Add Set\" button appends new rows, backward-compatible with plain workoutDetails, and saves structured SetRow arrays to the backend.",
  "frontend/src/pages/Progress.tsx": "Progress tracking page showing completed count, streak, completion rate, and scheduled stats in a grid, a weekly progress bar, a day-by-day breakdown with perfect week celebration, and a compact TierBadge in the header — updated to use `WorkoutSchedule` instead of the removed `WorkoutScheduleEntry` type.",
  "frontend/src/index.css": "Dark fitness aesthetic with OKLCH color system — fiery orange primary, dark charcoal background, Inter font, success/glow utility classes, and both light/dark mode tokens.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Barlow Condensed fonts, success color token, glow box shadows, fitness-themed color palette, and enhanced border radius scale.",
  "frontend/src/components/TierBadge.tsx": "Reusable tier badge component with `compact` and `full` variants, exports the 22-tier list, `getTierGroup`, and `getTierStyle` helpers with visually escalating color/glow styling per tier group.",
  "frontend/src/pages/Settings.tsx": "Settings page with a \"Workout Reminders\" notification toggle card, current tier display, weekly completion stats with advancement thresholds, tier evaluation button, and full tier reference grid; all `flex-shrink-0` classes updated to `shrink-0` and unused variable removed.",
  "frontend/public/sw.js": "Service Worker that handles 4-hour periodic workout reminder notifications using setInterval, displays motivational quotes from a built-in list, responds to START/STOP/TEST messages from the main app, and focuses or opens the app window when a notification is clicked.",
  "frontend/public/manifest.json": "Minimal PWA web app manifest for \"Daily Grind\" defining standalone display mode, theme/background colors, and the app icon to enable home screen installation and improve background notification delivery.",
  "frontend/src/utils/notificationService.ts": "Utility module that registers the service worker, checks/requests browser notification permission, and sends START/STOP/TEST messages to the active service worker to control the notification schedule.",
  "frontend/src/hooks/useNotifications.ts": "Custom React hook that manages notification permission state, syncs the enabled/disabled state from the user's backend profile on mount, and exposes enableNotifications/disableNotifications/sendTestNotification callbacks that update both the service worker schedule and the backend UserProfile.",
  "frontend/index.html": "HTML entry point with added PWA manifest link, theme-color meta tag, and Apple mobile web app meta tags to support home screen installation and notification behavior on iOS/Android."
}